git init (ine time only)
create your file (with command = $ touch filename.txt)
or right click to add files

to just know what is the changs
$ git status
if red
$ git add filename.txt
or
$ git add --all or git add .       (same)
else if green
$ git commit -m "some text that what you changed"

fol log 
$ git log

for change to old file as head
$ git checkout ( key 40 english letter coppy past)

for cteate new branch

$ git branch branch name

for switch the branches
$ git checkout branch name

now if you committed then change in the new branch name